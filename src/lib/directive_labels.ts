

// Immutable/forzen. this is because cahnges wont be reflected between client and server.
export const directive_labels = Object.freeze({
	callout: 'Callout',
	footnote: 'Footnote',
	code: 'Code block',
});